const math = 4;
const rus = 5;
const eng = 2;

if (math > 3 && rus > 3 && eng > 3) {
    console.log('You passed all the exames');
} else if (math > 3 || rus > 3 || end > 3) {
    console.log('Not all exames are passed');
} else {
    console.log('You have been expelled');
}